sudo: required

services:
  - docker
  
language: cpp

compiler:
- clang
- gcc

env:
- CMAKE_BUILD_TYPE=Debug
- CMAKE_BUILD_TYPE=Release

before_script:
  - docker build --tag my_image --build-arg CMAKE_BUILD_TYPE=${CMAKE_BUILD_TYPE} .

script:
  - docker run my_image ./kui_test
